import React from "react";

const Header = () => {
    return(
        <header className="header-title">
            <h2 className="main-web-title">Birthday Reminder</h2>
        </header>
    );
}

export default Header;